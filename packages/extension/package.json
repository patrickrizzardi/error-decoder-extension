{
  "name": "@error-decoder/extension",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "bun run --watch build.ts",
    "build": "bun run build.ts",
    "test": "bun test"
  },
  "dependencies": {
    "dompurify": "^3.3.3"
  },
  "devDependencies": {
    "@types/chrome": "latest",
    "@types/dompurify": "^3.2.0",
    "typescript": "latest"
  }
}
