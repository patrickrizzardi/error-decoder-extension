{
  "name": "@error-decoder/extension",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "bun run --watch build.ts",
    "build": "bun run build.ts",
    "test": "bun test"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/chrome": "latest",
    "typescript": "latest"
  }
}
